These two programs - CheckDuplicates and WordCount - are sample
clients for the IntSet and StringIntMap classes respectively.

To use these, copy them into them into the folder with the code
for the corresponding lab, along with Timer.java.  Then compile
and run them.
